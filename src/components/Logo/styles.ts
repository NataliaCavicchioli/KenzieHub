import styled from "styled-components";

const Container = styled.div`
  color: var(--primary);
  font-weight: 700;
  font-size: 2.3rem;
  margin: 0 0 2rem;
  text-align: center;
`;

export default Container;
