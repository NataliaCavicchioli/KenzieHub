import Logo from "../Logo";
import ContainerHeader from "./styles";

const Header = () => {
  return (
    <ContainerHeader>
      <Logo></Logo>
    </ContainerHeader>
  );
};

export default Header;
